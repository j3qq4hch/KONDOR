{
  "name": "kondor",
  "version": "0.1.0",
  "description": "PCB device assembly viewer",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build:main": "tsc -p tsconfig.main.json && copy src\\preload.cjs dist\\preload.cjs",
    "build:renderer": "vite build",
    "build": "npm run build:main && npm run build:renderer",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "@types/three": "^0.176.0",
    "electron": "^32.3.3",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "three": "^0.176.0"
  }
}
